<Window x:Class="CaptureIt.App.Settings.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CaptureIt Settings" Width="520"
        WindowStartupLocation="CenterScreen"
        SizeToContent="Height"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Save folder -->
        <GroupBox Grid.Row="0" Header="Save folder" Margin="0,0,0,12">
            <DockPanel Margin="8">
                <Button DockPanel.Dock="Right" Content="Browse..." Width="90" Margin="8,0,0,0" Click="OnBrowseFolderClick" />
                <TextBox x:Name="SaveFolderTextBox" VerticalContentAlignment="Center" />
            </DockPanel>
        </GroupBox>

        <!-- Filename pattern -->
        <GroupBox Grid.Row="1" Header="Filename pattern (.png always appended)" Margin="0,0,0,12">
            <StackPanel Margin="8">
                <TextBox x:Name="FilenamePatternTextBox" TextChanged="OnFilenamePatternTextChanged" />
                <TextBlock x:Name="FilenamePreviewText" Margin="0,6,0,0" Foreground="Gray" />
                <TextBlock Text="Tokens: {date}, {time}, {datetime}" Margin="0,2,0,0" FontSize="11" Foreground="Gray" />
            </StackPanel>
        </GroupBox>

        <!-- Hotkey -->
        <GroupBox Grid.Row="2" Header="Global hotkey (repeats last-used capture mode)" Margin="0,0,0,12">
            <StackPanel Margin="8">
                <TextBox x:Name="HotkeyTextBox" IsReadOnly="True"
                         PreviewKeyDown="OnHotkeyPreviewKeyDown"
                         Text="Click here and press a new key combination" />
                <TextBlock x:Name="HotkeyValidationText" Margin="0,6,0,0" Foreground="Red" />
            </StackPanel>
        </GroupBox>

        <!-- Options -->
        <GroupBox Grid.Row="3" Grid.RowSpan="2" Header="Options" Margin="0,0,0,12">
            <StackPanel Margin="8">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Capture delay:" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <ComboBox x:Name="CaptureDelayComboBox" Width="140" HorizontalAlignment="Left" />
                </StackPanel>
                <CheckBox x:Name="OcrEnabledCheckBox" Content="Extract text from captured screenshots" Margin="0,10,0,0" />
                <CheckBox x:Name="SaveToClipboardCheckBox" Content="Save to clipboard" Margin="0,10,0,0" />
            </StackPanel>
        </GroupBox>

        <!-- Notes -->
        <TextBlock Grid.Row="5" TextWrapping="Wrap" Foreground="Gray" FontSize="11" Margin="0,0,0,12"
                   Text="Note: CaptureIt does not start automatically with Windows. Screenshots are saved as PNG only. If the save folder becomes unavailable, captures automatically fall back to the Pictures\CaptureIt folder." />

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="90" Margin="0,0,8,0" Click="OnCancelClick" />
            <Button Content="Save" Width="90" Click="OnSaveClick" IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
